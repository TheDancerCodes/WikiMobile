package com.thedancercodes.wikimobile.models

// Represents query data that is returned. Contains a list of the page objects
class WikiQueryData {

    // An ArrayList of WikiPage instantiated as an empty list of ArrayList
    val pages: ArrayList<WikiPage> = ArrayList<WikiPage>()
}